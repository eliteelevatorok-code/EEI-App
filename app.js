
/* Transcribed exactly from the Form Master PDF (Elite  Elevator Inspections Form
   Master 03042025.pdf), pages 3-8. Item text and code citations are verbatim,
   including the source's own inconsistent spacing/typos - do not "fix" wording,
   do not add or complete entries. Only what's in that document. Ask Robert. */
const VIOLATION_STEPS = [
{ step:"STEP I", head:"Machine room inspection. This is where test, maintenance, monthly firefighter documents are located also elevator drive unit serial number and ID number.", items:[
"Access keys shall be provided -- 17.1-8.6.11.3,-MR",
"Remove blockage to access machinery space, machine room -- 17.1-2.7.3,-MR",
"Machine Room Doors must be kept locked and closed -- 17.1-8.6.4.8.4,-MR",
"Machine room doors must be self-closing, spring type self-locking door knob -- 17.1-2.7.3.4.1,-MR",
"Provide permanent means to access elevator machine room -- 17.3-2.2.2,-MR",
"Provide machine rooms to be ventilated natural or mechanical -- 17.3-2.2.4,-MR",
"Install light switch located at point of entry -- 17.1-2.7.9.1,-MR",
"Machine room must have lighting minimum illumination (19fc) -- 17.1-2.7.9.1,-MR",
"Machine room lighting required to be permanent -- 17.3-2.2.3,-MR",
"Machine numbering required for more than one elevator in building -- 17.1-2.29.1,-MR",
"Elevator light disconnect must be installed -- NEC 620.22 / 53,-MR",
"Mainline disconnect must be installed within sight of elevator -- NEC620.51,-MR",
"Multiple power source warning sign installed on mainline disconnect -- NEC620.52,-MR",
"Provide power supply line disconnect means at each elevator motor, or motor generator set and controller equipment -- 17.3-3.10.5,-MR",
"Disconnect must be lockable -- NEC620.51,-MR",
"Install ground fault circuit interrupter (GFCI) in pit and machine room -- NEC620.85,-MR",
"Install protective covering on non-elevator related piping in machinery room -- 17.3-2.2.5,-MR",
"All elevator equipment shall be protected from weather -- 17.3-2.2.6,-MR",
"Provide written maintenance control program -- 17.1-8.6.1.2.1,-MR",
"All maintenance records shall be maintained to accurately reflect the frequency of service and items serviced -- 17.1-8.6.1.4.1,-MR",
"Maintenance record availability shall be kept on site -- 17.1-8.6.1.4.2,-MR",
"Oil Log shall be maintained and available of hydraulic fluid when added to system -- 17.1-8.6.5.7,-MR",
"Provide means to check minimum and maximum hydraulic oil level -- 17.1-8.6.5.4,-MR",
"Provide or maintain monthly firefighter test log -- 17.1-8.6.11.1,-MR",
"Elevator shall conform to the requirements of firefighter service Phase I & II -- 17.3-3.11.3,-",
"Electrical schematic shall be available -- 17.1-8.6.1.6.3a,-MR",
"Clean inside elevator controller -- 17.1-8.6.1.6.3b,-MR",
"Remove temporary wiring blocks from controller -- 17.1-8.6.1.6.3c,-MR",
"Jumper Wires not to be stored in controller -- 17.1-8.6.1.6.3d,-MR",
"\"ABC\" fire extinguisher required in machine room located near access door -- 17.1-8.6.1.6.5,-MR",
"Periodic test tags shall be installed -- 17.1-8.6.1.7.3,-DOC",
"Relief valve shall be sealed -- 17.1-8.6.5.9,-MR",
"Relief valve shall not exceed 150% of working pressure -- 17.1-8.6.5.14,-MR",
"Provide test documentation on site -- 17.1-8.11.1.1.1,-DOC",
"Witness inspector must provide signed test forms -- 17.1-8.11.1.1.2a,-DOC",
"Provide glass back elevators and hoistway cleaning procedure -- 17.1-8.6.11.4,-DOC",
"Provide written emergency evacuation procedure from a stalled elevator -- 17.1-8.6.11.5,-DOC",
"Provide escalator start up procedure -- 17.1-8.6.11.6,-DOC",
"Periodic (1) year test electric elevator -- 17.1-8.6.4.19,-DOC",
"Five (5) year test electric elevator -- 17.1-8.6.4.19.7,-DOC",
"Periodic hydraulic elevator test -- 17.1-8.6.5.,-DOC",
"Standby Power where provided elevator operation shall be tested -- 17.1-8.6.4.19.7,-DOC",
"Install code data plate -- 17.1-8.9,-MR",
"Replace a worn belt, and/or chain (entire set must be replaced) -- 17.1-8.6.3.5,-MR",
"Provide means to control the ambient temperature in the machine room -- 17.1-2.7.6.3.2,-MR",
"Replace expired flexible hose & fittings no more than every six (6) years from installation date -- 17.1-8.6.5.6,-MR",
"One suspension rope required to be replaced (entire set must be replaced) -- 17.1-8.6.3.2,-MR",
"Winding drum, no less than 1 complete turn on drum -- 17.3-3.12.7,-MR",
"Minimum 3 ropes (traction) 2 ropes (drum type) required -- 17.3-3.12.4,-MR",
"Wire ropes (suspension/governor) shall be kept clean for inspection purposes -- 17.1-8.6 4.1.1,-MR",
"Governor ropes shall not be lubricated -- 17.1-8.6.4.2.1,-MR",
"Suspension wire ropes shall be lightly lubricated -- 17.1-8.6.4.2.1,-MR",
"Clear floors, machinery room & control spaces of water, oil & debris -- 17.1-8.6.4.8.1,-MR",
"Remove non-elevator items in machine room -- 17.1-8.6.4.8.2,-MR",
"Flammable liquids with flash point of less than 110 degrees F, shall not be stored in machine room -- 17.1-8.6.4.8.3,-MR"
]},
{ step:"STEP II", head:"Pit inspection. Check pit access device function, pit stop switch function and properly identified, buffers in place, pit access ladder securely mounted, pit dry free of debris, oil, water, elevator parts.", items:[
"Hoistway unlocking device shall be provided -- 17.3- 2.7.4,-PIT",
"Single receptacle for sump pump (non-GFCI) shall be installed -- NEC620.24,-PIT",
"Pit access doors shall be kept closed and locked -- 17.1-8.6.4.7.3,-PIT",
"Provide pit light switch, not connected to load side of GFCI receptacle, located next to pit access (10fc) -- NEC620.24,-PIT",
"Pit stop switch shall be located adjacent to the nearest point of access pits exceeds 67\" in depth addition stop switch is required-17.1-2.2.6,-PIT",
"Install pit stop switch, switch must be properly labeled -- 17.3-2.3.3,-PIT",
"Hydraulic packing shall be maintained to prevent excessive loss of fluid -- 17.1- 8.6.5.5.1,-PIT",
"Single Bottom Jacks requirements car safeties or plunger gripper -- 17.1-8.6.5.8,-PIT",
"Install car buffer -- 17.1- 8.11.3.1.5,-PIT",
"Counterweight metal guards shall be installed -- 17.3-2.1.5,-PIT",
"Maximum runby, car – 24 inches, counterweight – 36 inches -- 17.3-2.4.3,-PIT",
"Minimum and Maximum runby (see code requirements) -- 17.1-2.4.2,-PIT",
"Water & oil shall not be allowed to accumulate in pit -- 17.1-8.6.4.7.4,-PIT",
"Remove dirt and debris from pit -- 17.1-8.6.4.7.1,-PIT",
"Drains connected directly to sewer not allowed -- 17.3-2.3.2,-PIT",
"Install oil collection container -- 17.1-8.6.5.5.2,-PIT",
"Pit light protection -- 17.1-2.2.5.2,-PIT",
"Sump pump -- 17.1-2.2.2.5,-PIT",
"Sump cover -- 17.2-2.2.2.6,-PIT",
"Restore pit lighting -- 17.3-2.2.3,-PIT"
]},
{ step:"STEP III", head:"Hoistway inspection. After taking control of elevator, check function hoistway door intertlocks mechanism, door gibbs loose or missing, door restrictor function, hoitway door numbering, hoistway door self-closing, hoistway ledges more than 4\" wide, condition of hoistway for penetrations, holes and cracks.", items:[
"Hoistway unlocking devices shall be provide at all landing for emergency purposes -- 17.3-2.7.4,-HOIST",
"Hoistway construction shall be enclosed and all penetrations to be fire resistive sealed -- 17.3-2.1.1,-HOIST",
"Hoistway floor numbering on hoistway doors on hoistway side with 4\" numbers -- 17.1-2.29.2 also 17.1-8.11.2.1.3(o),-HOIST",
"Hoistway enclosure using glass must be etched and visible on each panel with Z97.1/16CFR1201 -- 17.1- 2.1.1.2,-HOIST",
"Windows used for hoistway enclosure must be guarded -- 17.3-2.1.2,-HOIST",
"Ledges in hoistway more than 4\" wide must be beveled 60 to 75 degree -- 17.3-2.1.3 also 17.1-2.1.6,-HOIST",
"Existing piping, ducts and wiring in hoistway that cannot be removed shall be secured and covered to prevent interference with elevator operation-17.3-2.1.4",
"Counter weights tie-rods shall have lock nuts at each end secured by cotter pins -- 17.1-4.3.17,-HOIST",
"Pipes and/or ducts in hoistway which if discharged into the hoistway could be hazardous shall not be permitted -- 17.3-2.1.4,-HOIST",
"Repair car top lighting -- 17.3-2.2.3,-CT",
"Install car top light guards -- 17.1-2.14.7.1.4,-CT",
"Clear car top of debris and or spare parts -- 17.1-8.6.4.8.9,-CT",
"Minimum car top clearance shall not be less 43\"and no case less 24\" from crosshead, -- 17.1-2.4.7,-CT",
"Install car top standard railing on all outside perimeter in access of 12\" from hoistway walls -- 17.1-2.10.2,-CT",
"Install car top ground fault circuit interrupter (GFCI) -- NEC620.85,-CT",
"Provide car top emergency exit -- 17.3-3.4.4.1,-CT",
"Side emergency exit shall be lockable -- 17.3-3.4.4.2,-CT",
"Install car top operating device -- 17.3-3.10.3,-CT",
"Install wire rope data tag -- 17.3-3.12.2,-CT/MR",
"Install anti-rotation device (cable hobbles) -- 17.1-8.6.4.1.3,-CT",
"Perform rail lubrication maintenance according to crosshead, if applicable -- 17.1-8.6.4.3.1,-HOIST",
"Rails shall be free of lint and dirt -- 17.1-8.6.4.3.4,-HOIST",
"Shall be free of dirt and rubbish, no storage -- 17.1-8.6.4.7,-HOIST/PIT",
"Unenclosed hoistways, top emergency exits shall be permanently secured closed -- 17.3-3.4.4.1(b)1,-CT",
"Wire in hoistway shall be installed in conduit where required by NEC 620.21"
]},
{ step:"STEP IV", head:"Car Enclosure. Check two-way communication, emergency alarm, illumination, door reopening device function, door closing pressure, control panel function.", items:[
"Restore car emergency 2-way communication -- 17.1-8.6.11.2,-CAR",
"Restore car emergency alarm -- 17.3-3.11.1,-CAR",
"Capacity plate shall be installed -- 17.3- 3.7.4,-CAR",
"Freight elevator signage shall be installed -- 17.3- 3.7.5,-CAR",
"Door closing force shall not be more than 30ft-lbs -- 17.3- 2.8.1,-CAR",
"Glass enclosures shall be etched on each panel visible with Z97.1, 16 CFR 1201 -- 17.1-2.1.1.2.2,-CAR",
"Restore or replace door reopening device -- 17.3- 2.8.2,-CAR",
"Horizontally sliding, multisection, and horizontally swinging doors must have door vision panel -- 17.3-2.6.3,-CAR",
"Freight door pull straps must be installed bottom of strap not more than 79\"above landing(no length extensions to be added)-17.3-2.6.6,-CAR",
"Door restrictor shall prevent doors from opening outside unlocking zone no more than 4\" -- 17.1-2.14.5.7,-CAR",
"Door restrictor shall be installed -- 17.3-2.7.5,-CAR",
"Car lighting passenger (5fc), freight 2.5 (fc) shall be installed no less than two lamps shall be provided -- 17.3-3.4.5,-CAR",
"In car light shall be protected in the event of accidental breakage -- 17.3-3.4.6,-CAR",
"Restore or install emergency auxiliary light (0.2 fc) -- 17.3-3.4.5,-CAR",
"Restore emergency alarm -- 17.3-3.11.1,-CAR",
"Car emergency signaling devices shall not be less than 80 dBA nor greater than 90 dBA -- 17.1-2.27.1,-CAR",
"In car stop switch change to keyed or behind locked panel -- 17.3-3.10.4 r,-CAR"
]},
{ step:"STEP V", head:"Miscellaneous requirements.", items:[
"Restore firefighter service Phase I & II -- 17.1-2.27.3,-FF",
"Fire alarm initiating devices shall recall elevator to proper landing when activated -- 17.1-2.27.3.2,-FF",
"Require signs and data plates damaged or illegible must be replaced -- 17.1-8.6.1.6.7,-MISC",
"Install sign to info passengers not to use elevator in case of fire -- 17.1-2.27.9,-MISC",
"Car Emergency Systems shall be maintained (lights, communication and ventilation) -- 17.1-8.6.4.15,-MISC",
"Installation of sprinklers in machine room or hoistway is an alteration -- 17.3-2.1.4,-MISC",
"Notice unusual noise, and or irregular motion during car ride -- 17.1-8.11.2.1.1,-MISC",
"Elevator shall be maintained to provide stopping accuracy at the landing -- 17.1-8.6.4.16,-MISC",
"To maintain machine room fire rating, all ceiling tiles must to be in their design position -- IBC 3001,-MISC",
"Certificate of Operation shall be posted, in the elevator car or main lobby near call station -- Ok 380:70-3-8,-OESA",
"All elevators and conveyances shall provide an written Maintenance Control Program -- OK380:70-11-13,-OESA"
]},
{ step:"ADDED", head:"Entered by Robert after the printed list, via the form's own \"add to drop-down\" save button (page 6 of the Form Master).", items:[
"Requires firefighter service to be installed to all elevators that travel more then twenty-five(25) feet 17.3-3.11.3"
]}
];
const VIOLATIONS = VIOLATION_STEPS.flatMap(s=>s.items);

const CARRY = [
 {k:"serial",l:"Serial number"},{k:"permit",l:"Permit #"},
 {k:"locAttn",l:"Location attention"},{k:"locCounty",l:"Location county"},
 {k:"locAddr",l:"Location physical address"},{k:"locCity",l:"Location city"},
 {k:"locZip",l:"Location zip"},{k:"locEmail",l:"Location email"},
 {k:"owner",l:"Owner"},{k:"ownerAttn",l:"Owner attention"},{k:"ownerAddr",l:"Owner address"},
 {k:"ownerCity",l:"Owner city"},{k:"ownerState",l:"Owner state"},{k:"ownerZip",l:"Owner zip"},
 {k:"billName",l:"Billing contractor name"},{k:"billCity",l:"Billing contractor city"},
 {k:"billAttn",l:"Billing contractor attention"},{k:"nature",l:"Nature of business"},
 {k:"entity",l:"Entity type",opts:["Private","City","County","State"]},
 {k:"hoistLoc",l:"Hoistway location"},{k:"deviceLoc",l:"Device location in hoistway"},
 {k:"mfr",l:"Manufacturer"},{k:"capacity",l:"Capacity (lbs)"},{k:"speed",l:"Speed (FPM)"},
 {k:"rise",l:"Rise (ft in)"},{k:"openings",l:"Openings"},{k:"landings",l:"# of landings"},
 {k:"deviceType",l:"Device type",opts:["Const/Temp","Escalator/MW","Personnel Hoist","Platform Lift","Stairway Chair Lift","Wheelchair Lift","Elevator","LU/LA","Passenger","Freight"]},
 {k:"installed",l:"Installed year"},{k:"codeYear",l:"Code year"},
 {k:"machineType",l:"Machine type",opts:["Cable","Hand Powered","Roped Hydraulic","Direct Plunger Hydraulic","Other"]}
];

const GROUPS = {
 inspType:["Initial","Periodic","Reinsp","Follow-up","Witness","Alteration"],
 inspCycle:["1","2","3","Res"],
 certIssue:["Yes","No"], certTemp:["Yes","No"], tempDays:["30","60"],
 certTo:["Owner","Location"],
 cond:["No adverse conditions","Red Tag","Inactive","Scrapped"]
};

/* Plain labels, nothing automatic. Picking "Hospital" on an elevator doesn't
   change any other field - it's a note to self, visible later, so Robert
   remembers which rule he's working under without re-deriving it each time. */
const TAG_SUGGESTIONS = ["Hospital","Nursing Home","Belt","Gen 2","Screw Drive"];

/* Bump this on every deploy - it's the only way to tell which build is
   actually running on a given phone/computer. */
const APP_VERSION = "2026-08-27 14:40";

const PIN = "0508";

/* ---------- sync between devices (Cloudflare Worker + KV) ----------
   Fill in SYNC_URL once the Worker in worker/sync-worker.js is deployed
   (see worker/SETUP.md). Until then this is a no-op and the app behaves
   exactly like before it - one copy of the data, local to whichever
   device you're using.

   Whole-object last-write-wins: whichever device syncs most recently
   wins. Fine for how this actually gets used - field entry on the phone,
   computer work after - not fine for the same report being edited on
   both devices at once. Protected by the same 0508 code the app already
   uses, sent as a header. That is deliberately simple, not real
   security - good enough for keeping this private between Robert's own
   devices, nothing more. */
const SYNC_URL = "https://eei-sync.elite-elevator-ok.workers.dev";
const SYNC_TS_KEY = "eei_sync_ts_v1";
function getSyncTs(k){ try{ return (JSON.parse(localStorage.getItem(SYNC_TS_KEY)||"{}"))[k]||0; }catch(e){ return 0; } }
function setSyncTs(k,ts){ try{ const m=JSON.parse(localStorage.getItem(SYNC_TS_KEY)||"{}"); m[k]=ts; localStorage.setItem(SYNC_TS_KEY,JSON.stringify(m)); }catch(e){} }
function syncPush(key,data){
  if(!SYNC_URL || !navigator.onLine) return;
  const ts=Date.now();
  fetch(`${SYNC_URL}/${key}`,{
    method:"POST",
    headers:{"Content-Type":"application/json","X-EEI-PIN":PIN},
    body:JSON.stringify({data,updatedAt:ts})
  }).then(()=>setSyncTs(key,ts)).catch(()=>{ /* offline or unreachable - local copy is still safe */ });
}
async function syncPull(key){
  if(!SYNC_URL) return null;
  try{
    const res=await fetch(`${SYNC_URL}/${key}`,{headers:{"X-EEI-PIN":PIN}});
    if(!res.ok) return null;
    const text=await res.text();
    if(!text||text==="null") return null;
    return JSON.parse(text);
  }catch(e){ return null; }
}
async function syncBoot(){
  if(!SYNC_URL) return;
  const [cloudDb,cloudRoster]=await Promise.all([syncPull("db"),syncPull("roster")]);
  let changed=false;
  if(cloudDb && cloudDb.updatedAt>getSyncTs("db")){
    db=cloudDb.data||{}; writeDb(); changed=true;
  } else if(Object.keys(db).length){
    syncPush("db",db);
  }
  if(cloudRoster && cloudRoster.updatedAt>getSyncTs("roster")){
    setRoster(cloudRoster.data||[]); changed=true;
  } else if(ELEVATORS.length){
    syncPush("roster",ELEVATORS);
  }
  if(changed){
    boot();
    if(cur) pick(cur.okla);
    setStatus("Synced with your other device.","ok");
  }
}

const $ = id => document.getElementById(id);
const esc = s => String(s==null?"":s).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");

/* ---------- storage that never dies silently ---------- */
const STORE="eei_field_v2"; let STORAGE_OK=true;
function loadDb(){ try{ return JSON.parse(localStorage.getItem(STORE)||"{}"); }catch(e){ STORAGE_OK=false; return {}; } }
function writeDb(){ try{ localStorage.setItem(STORE,JSON.stringify(db)); }catch(e){ STORAGE_OK=false; return false; } syncPush("db",db); return true; }
let db = loadDb();

let cur=null, rep=null, acct=null, phoneMode=false;

/* ---------- the roster ----------
   Order of preference:
     1. a roster this device already imported (kept in local storage)
     2. elevators.js, if it was deployed alongside the app
     3. nothing - the app asks for the spreadsheet on first run
   Keeping the records out of the hosted files means the public URL carries
   no customer names, emails or phone numbers. -------------------------- */
const ROSTER_KEY = "eei_roster_v1";
let ELEVATORS = [];
try{ const s = localStorage.getItem(ROSTER_KEY); if(s) ELEVATORS = JSON.parse(s) || []; }catch(e){}
if(!ELEVATORS.length && Array.isArray(window.ELEVATORS)) ELEVATORS = window.ELEVATORS;

let ACCOUNTS = [];
function buildAccounts(){
  const m = new Map();
  ELEVATORS.forEach(e=>{
    const key = (e.email||"").toLowerCase() || ("no-email::"+(e.contact||e.loc));
    if(!m.has(key)) m.set(key,{key, name:e.contact||"(no contact name)", email:e.email||"", units:[]});
    m.get(key).units.push(e);
  });
  return [...m.values()].map(a=>{
    /* Strip a trailing elevator/car tag (" #2", " Elevator #2", " PE-1", " FE-1")
       to get the actual building name - "Google 5A PE-1" and "Google 5A FE-1"
       are two elevators in the same building, not two buildings. Label the
       account by its most common building name, shortest wins a tie. */
    const stems = a.units.map(u=>
      u.loc.replace(/\s*(?:elevator\s*)?#\s*[A-Za-z0-9]{1,4}\s*$/i,"")
           .replace(/\s+[A-Za-z]{1,3}-\d+\s*$/,"")
           .trim()
    ).filter(Boolean);
    const freq = {}; stems.forEach(s=> freq[s]=(freq[s]||0)+1 );
    a.label = Object.keys(freq).sort((x,y)=> freq[y]-freq[x] || x.length-y.length)[0] || a.name;
    a.sites = new Set(stems).size;
    return a;
  }).sort((a,b)=> b.units.length - a.units.length);
}
function setRoster(list){
  ELEVATORS = list;
  try{ localStorage.setItem(ROSTER_KEY, JSON.stringify(list)); }catch(e){ STORAGE_OK=false; }
  ACCOUNTS = buildAccounts();
  syncPush("roster", list);
}

/* Reads their spreadsheet exported as CSV. Column order is the one their sheet uses:
   Oklahoma #, Location, Date Inspected, Date Due, Contact, email, Phone, maintenance, note */
function parseCsv(text){
  const NL=String.fromCharCode(10), CR=String.fromCharCode(13);
  const rows=[]; let row=[], cell="", q=false;
  for(let i=0;i<text.length;i++){
    const ch=text[i];
    if(q){
      if(ch==='"'){ if(text[i+1]==='"'){ cell+='"'; i++; } else q=false; }
      else cell+=ch;
    } else if(ch==='"') q=true;
    else if(ch===","){ row.push(cell); cell=""; }
    else if(ch===NL){ row.push(cell); rows.push(row); row=[]; cell=""; }
    else if(ch!==CR) cell+=ch;
  }
  if(cell.length||row.length){ row.push(cell); rows.push(row); }
  const out=[];
  rows.forEach(r=>{
    const c = r.map(x=>String(x||"").trim());
    if(c.length<4) return;
    if(!/^\d+$/.test(c[0])) return;                    // skips the header row and any blanks
    if(!/^\d+\/\d+\/\d{4}$/.test(c[3])) return;        // skips SCRAPPED / INACTIVE / MOD
    out.push({okla:c[0], loc:c[1], last:c[2], due:c[3], contact:c[4]||"",
              email:c[5]||"", phone:c[6]||"", prov:c[7]||"", note:c[8]||""});
  });
  return out;
}

/* ---------- PIN gate ---------- */
let entry="";
function drawDots(bad){
  const d=$("dots"); d.className="dots"+(bad?" bad":"");
  [...d.children].forEach((el,i)=> el.className = i<entry.length ? "on":"" );
}
$("pad").addEventListener("click", e=>{
  const b=e.target.closest("button"); if(!b) return;
  const act=b.dataset.act;
  if(act==="clear") entry="";
  else if(act==="back") entry=entry.slice(0,-1);
  else if(entry.length<4) entry+=b.textContent.trim();
  $("gateMsg").textContent="";
  drawDots(false);
  if(entry.length===4){
    if(entry===PIN){ try{ sessionStorage.setItem("eei_ok","1"); }catch(e){} unlock(); }
    else { $("gateMsg").textContent="That code is not right. Try again."; drawDots(true); entry=""; setTimeout(()=>drawDots(false),700); }
  }
});
function unlock(){ $("gate").classList.add("hide"); $("shell").classList.remove("hide"); boot(); syncBoot();
  if($("verfoot")) $("verfoot").textContent = `Build ${APP_VERSION}`;
}

/* ---------- text size (slider, not steps - the whole UI is built in rem
   off this one root value, so this scales everything together) ---------- */
const SIZE_KEY = "eei_size_v1";
function applySize(px){
  document.documentElement.style.setProperty("--fs", px+"px");
  try{ localStorage.setItem(SIZE_KEY, px); }catch(e){}
}
(function initSize(){
  let px = 20;
  try{ const s = localStorage.getItem(SIZE_KEY); if(s) px = +s; }catch(e){}
  applySize(px);
  if($("sizeSlider")){
    $("sizeSlider").value = px;
    $("sizeSlider").addEventListener("input", e=> applySize(+e.target.value));
  }
})();
function lockApp(){ try{ sessionStorage.removeItem("eei_ok"); }catch(e){}
  entry=""; drawDots(false); $("gate").classList.remove("hide"); $("shell").classList.add("hide"); }
try{ if(sessionStorage.getItem("eei_ok")==="1") unlock(); }catch(e){}

/* ---------- phone vs computer ----------
   Mode is decided by screen size, nothing to switch by hand. Phone = the field
   (carried-over equipment fields locked so nothing gets changed by accident);
   computer = the desk (those fields editable). */
const calcMode=()=> window.innerWidth<900;
function applyMode(){
  phoneMode=calcMode();
  $("modeTag").textContent = phoneMode?"Phone · field":"Computer";
  $("modeTag").className = "pill"+(phoneMode?" field":"");
  $("lockBar").classList.toggle("hide", !phoneMode);
  document.querySelectorAll("#carryFields input:not([data-fixed]), #carryFields select").forEach(el=>{
    if(phoneMode){ el.setAttribute("readonly","readonly"); el.setAttribute("disabled","disabled"); }
    else { el.removeAttribute("readonly"); el.removeAttribute("disabled"); }
  });
  $("wholeListBtn").classList.toggle("hide", phoneMode);
  if(cur) renderTags();
}
window.addEventListener("resize",applyMode);

/* ---------- two stage picker ---------- */
document.querySelectorAll(".stagehead").forEach(h=>{
  h.onclick=()=>{ const s=$(h.dataset.stage);
    if(h.dataset.stage==="stUnit" && !acct){ openStage("stAcct"); return; }
    s.classList.contains("open") ? s.classList.remove("open") : openStage(h.dataset.stage); };
});
function openStage(id){
  ["stAcct","stUnit"].forEach(s=> $(s).classList.toggle("open", s===id));
  if(id==="stAcct"){ $("acctSearch").value=""; renderAccts(""); }
  if(id==="stUnit"){ $("unitSearch").value=""; renderUnits(""); }
}
function renderAccts(q){
  q=(q||"").trim().toLowerCase();
  const hits=ACCOUNTS.filter(a=> !q || a.label.toLowerCase().includes(q) || a.name.toLowerCase().includes(q) || a.email.toLowerCase().includes(q));
  $("acctList").innerHTML = hits.slice(0,60).map(a=>
    `<button class="opt" data-acct="${esc(a.key)}"><span class="t">${esc(a.label)}</span>
     <span class="s">${esc(a.name)} &middot; <span class="n">${a.units.length}</span> elevator${a.units.length===1?"":"s"} &middot; ${a.sites} building${a.sites===1?"":"s"}</span></button>`
  ).join("") || `<p class="empty">No account matches that.</p>`;
  $("acctHint").textContent = `${ACCOUNTS.length} accounts, grouped by contact — the only grouping in their spreadsheet today. Swap this for the real account column once it is added.`;
}
$("acctSearch").addEventListener("input",e=>renderAccts(e.target.value));
$("acctList").addEventListener("click",e=>{
  const b=e.target.closest("[data-acct]"); if(!b) return;
  acct=ACCOUNTS.find(a=>a.key===b.dataset.acct);
  $("acctVal").textContent=acct.label; $("acctVal").classList.remove("empty");
  $("stAcct").classList.add("done");
  cur=null; rep=null; $("plate").classList.remove("on"); showReport(false);
  $("unitVal").textContent="Choose an elevator"; $("unitVal").classList.add("empty");
  $("stUnit").classList.remove("done");
  openStage("stUnit");
});
function renderUnits(q){
  if(!acct){ $("unitList").innerHTML=""; return; }
  q=(q||"").trim().toLowerCase();
  const hits=acct.units.filter(u=> !q || u.loc.toLowerCase().includes(q) || u.okla.toLowerCase().includes(q));
  hits.sort((a,b)=> a.loc.localeCompare(b.loc) || a.okla.localeCompare(b.okla));
  $("unitList").innerHTML = hits.map(u=>
    `<button class="opt" data-unit="${esc(u.okla)}"><span class="t">${esc(u.loc)}</span>
     <span class="s"><span class="n">#${esc(u.okla)}</span> &middot; due ${esc(u.due)} &middot; ${esc(u.prov||"no maintenance company on file")}</span></button>`
  ).join("") || `<p class="empty">Nothing in this account matches.</p>`;
}
$("unitSearch").addEventListener("input",e=>renderUnits(e.target.value));
$("unitList").addEventListener("click",e=>{
  const b=e.target.closest("[data-unit]"); if(!b) return;
  pick(b.dataset.unit);
  $("stUnit").classList.remove("open"); $("stUnit").classList.add("done");
});

/* ---------- whole list (computer only) ----------
   The field picker narrows to one account at a time on purpose - this is the
   opposite: every elevator loaded on this device, in one scrolling list, so
   Robert can scan across accounts, see due dates and tags at a glance, and
   jump straight to one without going through account first. */
function hasLastYear(okla){ return !!((db[okla]||{}).archived); }
function renderWhole(q){
  q=(q||"").trim().toLowerCase();
  const hits=ELEVATORS.filter(u=>{
    if(!q) return true;
    const tags=getTags(u.okla).join(" ").toLowerCase();
    const acctLabel=(ACCOUNTS.find(a=>a.units.some(x=>x.okla===u.okla))||{}).label||"";
    return u.okla.toLowerCase().includes(q) || u.loc.toLowerCase().includes(q) ||
      acctLabel.toLowerCase().includes(q) || tags.includes(q);
  });
  hits.sort((a,b)=> a.loc.localeCompare(b.loc) || a.okla.localeCompare(b.okla));
  $("wholeBody").innerHTML = hits.map(u=>{
    const tags=getTags(u.okla), late=dueLate(u.due), ly=hasLastYear(u.okla);
    return `<button class="wholerow" data-pick="${esc(u.okla)}">
      <span class="t">${esc(u.loc)}</span> <span class="n">#${esc(u.okla)}</span>
      <span class="ly ${ly?"on":"off"}">${ly?"Last year on file":"No last year on file"}</span>
      <div class="s">Due <span class="${late?"late":""}">${esc(u.due)}</span> &middot; ${esc(u.prov||"no maintenance company on file")}</div>
      ${tags.length?`<div>${tags.map(t=>`<span class="tg">${esc(t)}</span>`).join("")}</div>`:""}
    </button>`;
  }).join("") || `<p class="empty">Nothing matches.</p>`;
  const withLY = ELEVATORS.filter(u=>hasLastYear(u.okla)).length;
  $("wholeSummary").textContent = `${withLY} of ${ELEVATORS.length} elevators have last year's report on file.`;
}
function pickFromWhole(okla){
  acct=ACCOUNTS.find(a=>a.units.some(u=>u.okla===okla));
  if(acct){
    $("acctVal").textContent=acct.label; $("acctVal").classList.remove("empty");
    $("stAcct").classList.add("done"); $("stAcct").classList.remove("open");
  }
  pick(okla);
  $("stUnit").classList.add("done"); $("stUnit").classList.remove("open");
  $("wholeSheet").classList.remove("on");
}
$("wholeListBtn").onclick=()=>{ if(!ELEVATORS.length) return;
  $("wholeSearch").value=""; renderWhole(""); $("wholeSheet").classList.add("on"); };
$("wholeClose").onclick=()=>$("wholeSheet").classList.remove("on");
$("wholeSearch").addEventListener("input",e=>renderWhole(e.target.value));
$("wholeBody").addEventListener("click",e=>{
  const b=e.target.closest("[data-pick]"); if(!b) return;
  pickFromWhole(b.dataset.pick);
});

/* ---------- select an elevator ---------- */
function dueLate(d){ const p=String(d).split("/"); if(p.length!==3) return false;
  return new Date(+p[2],+p[0]-1,+p[1]) < new Date(); }
/* The report form (dates, violations, notes, carried over) only shows once an
   account AND a specific elevator have both been picked - never a blank form
   that isn't tied to anything. */
function showReport(on){
  $("mainReport").classList.toggle("hide",!on);
  $("actionbar").classList.toggle("hide",!on);
}
function pick(okla){
  cur=ELEVATORS.find(x=>x.okla===okla); if(!cur) return;
  const saved=(db[okla]||{}).current;
  const archived=(db[okla]||{}).archived;
  rep = saved || {
    viol: archived ? JSON.parse(JSON.stringify(archived.viol)) : [],
    groups:{},
    carry: archived ? JSON.parse(JSON.stringify(archived.carry||{})) : {},
    /* Test dates often don't change year to year (the 5-year test only
       happens once every 5 years) - carry them forward as a starting point,
       editable if this visit actually redid one. Date inspected / cert
       expires are NOT carried - those are this visit's own dates and a
       stale one showing here could get submitted by accident. */
    dateInsp:"", certExp:"",
    test1: archived ? (archived.test1||"") : "",
    test5: archived ? (archived.test5||"") : "",
    notes:""
  };
  $("unitVal").textContent=cur.loc; $("unitVal").classList.remove("empty");
  const late=dueLate(cur.due);
  $("plate").innerHTML =
    `<div class="pk">Oklahoma number</div><div class="pn">${esc(cur.okla)}</div>
     <div class="pb">${esc(cur.loc)}</div>
     <div class="pm"><b>Contact</b> ${esc(cur.contact||"none on file")} &middot; ${esc(cur.email||"no email")}<br>
     <b>Maintenance</b> ${esc(cur.prov||"none on file")} &middot; <b>Last inspected</b> ${esc(cur.last||"not recorded")}</div>
     <div class="duechip${late?" late":""}">${late?"Past due":"Due"} ${esc(cur.due)}</div>`;
  $("plate").classList.add("on");
  showReport(true);
  renderAll();
  setStatus(saved?"Report already started on this device":"New report started","ok");
}

/* ---------- status ---------- */
function setStatus(t,kind){ $("status").textContent=t;
  $("stDot").className="dot"+(kind==="wait"?" wait":kind==="bad"?" bad":""); }
function save(){
  if(!cur) return;
  db[cur.okla]=db[cur.okla]||{}; db[cur.okla].current=rep; writeDb();
  if(!STORAGE_OK) return setStatus("This browser is blocking storage. Nothing is being kept.","bad");
  if(!SYNC_URL) return setStatus("Saved on this device.","ok");
  setStatus(navigator.onLine?"Saved and synced":"Saved on this phone. It will sync on wifi.",navigator.onLine?"ok":"wait");
}

/* ---------- chip groups ---------- */
function renderGroups(){
  for(const id in GROUPS) $(id).innerHTML = GROUPS[id].map(o=>
    `<button data-g="${id}" data-v="${esc(o)}" class="${rep.groups[id]===o?"on":""}">${esc(o)}</button>`).join("");
}
document.addEventListener("click",e=>{
  const b=e.target.closest("button[data-g]"); if(!b||!rep) return;
  const g=b.dataset.g,v=b.dataset.v;
  rep.groups[g]= rep.groups[g]===v ? null : v;
  renderGroups(); save();
});

/* ---------- carried over ---------- */
function renderCarry(){
  const archived=(db[cur.okla]||{}).archived;
  $("carryFields").innerHTML =
    `<div class="f"><label>Oklahoma number</label><input value="${esc(cur.okla)}" readonly data-fixed></div>
     <div class="f"><label>Location (building)</label><input value="${esc(cur.loc)}" readonly data-fixed></div>
     <div class="lockbar"><b>From their spreadsheet</b><span>Check these against the front desk while you're there — the app can't tell you if they've changed.</span></div>
     <div class="f"><label>Contact</label><input value="${esc(cur.contact||"none on file")}" readonly data-fixed></div>
     <div class="f"><label>Contact email</label><input value="${esc(cur.email||"none on file")}" readonly data-fixed></div>
     <div class="f"><label>Contact phone</label><input value="${esc(cur.phone||"none on file")}" readonly data-fixed></div>
     <div class="f"><label>Maintenance company</label><input value="${esc(cur.prov||"none on file")}" readonly data-fixed></div>
     <div class="f"><label>Spreadsheet note</label><input value="${esc(cur.note||"—")}" readonly data-fixed></div>
     <div class="lockbar"><b>${archived?"From last year's report":"Nothing on file yet"}</b><span>${archived?"Carried forward from the last finalized report. Edit on the computer if anything's changed.":"This elevator hasn't been finalized on this device before, so there's nothing to carry forward yet."}</span></div>` +
    CARRY.map(f=>{
      const val=rep.carry[f.k]||"";
      const ctl = f.opts
        ? `<select data-c="${f.k}"><option value="">&mdash;</option>`+f.opts.map(o=>`<option ${val===o?"selected":""}>${esc(o)}</option>`).join("")+`</select>`
        : `<input data-c="${f.k}" value="${esc(val)}">`;
      return `<div class="f"><label>${esc(f.l)}</label>${ctl}</div>`;
    }).join("");
  $("carryFields").querySelectorAll("[data-c]").forEach(el=>
    el.addEventListener("change",()=>{ rep.carry[el.dataset.c]=el.value; save(); }));
}

/* ---------- tags ----------
   Plain labels stuck on an elevator (by Oklahoma number, so they stick across
   years, not per-report). No field in the app reads them automatically - they
   exist so Robert can see, later, which rule he's working under. */
function getTags(okla){ return (db[okla]||{}).tags || []; }
function saveTags(okla, tags){ db[okla]=db[okla]||{}; db[okla].tags=tags; writeDb(); }
function renderTags(){
  const tags = getTags(cur.okla);
  const chips = tags.map(t=>
    `<span class="tagchip">${esc(t)}${phoneMode?"":`<button data-untag="${esc(t)}" title="Remove">&times;</button>`}</span>`
  ).join("");
  const suggest = phoneMode ? "" : TAG_SUGGESTIONS.filter(t=>!tags.includes(t)).map(t=>
    `<span class="tagsuggest"><button data-addtag="${esc(t)}">+ ${esc(t)}</button></span>`).join("");
  const custom = phoneMode ? "" :
    `<span class="tagadd"><input id="tagCustom" placeholder="Custom tag"><button id="tagCustomAdd">Add</button></span>`;
  $("tagBar").innerHTML = chips + suggest + custom;
  $("tagBar").classList.toggle("on", tags.length>0 || !phoneMode);
}
document.addEventListener("click", e=>{
  if(!cur) return;
  const rm=e.target.closest("[data-untag]");
  if(rm){ saveTags(cur.okla, getTags(cur.okla).filter(t=>t!==rm.dataset.untag)); renderTags(); return; }
  const add=e.target.closest("[data-addtag]");
  if(add){ const t=add.dataset.addtag; const tags=getTags(cur.okla);
    if(!tags.includes(t)) saveTags(cur.okla, [...tags, t]); renderTags(); return; }
  if(e.target.id==="tagCustomAdd"){
    const inp=$("tagCustom"); const t=(inp.value||"").trim(); if(!t) return;
    const tags=getTags(cur.okla);
    if(!tags.includes(t)) saveTags(cur.okla, [...tags, t]); renderTags();
  }
});

/* ---------- violations ---------- */
const splitV = s => { const i=s.lastIndexOf(" -- ");
  return i<0?{t:s,c:""}:{t:s.slice(0,i),c:s.slice(i+4)}; };
function renderViol(){
  $("vlist").innerHTML = rep.viol.map((v,i)=>{ const p=splitV(v.text);
    return `<div class="vrow" data-k="${v.kind}"><p>${esc(p.t)}<code>${esc(p.c)}</code></p>
      <div class="vbar">
        <button data-vi="${i}" data-k="V" class="${v.kind==="V"?"on":""}">V</button>
        <button data-vi="${i}" data-k="R" class="${v.kind==="R"?"on":""}">R</button>
        <button data-vi="${i}" data-k="C" class="${v.kind==="C"?"on":""}">C</button>
        <button data-vi="${i}" data-k="X">Remove</button>
      </div></div>`; }).join("") || `<p class="empty">Nothing added yet.</p>`;
  $("cV").textContent=rep.viol.filter(v=>v.kind==="V").length;
  $("cR").textContent=rep.viol.filter(v=>v.kind==="R").length;
  $("cC").textContent=rep.viol.filter(v=>v.kind==="C").length;
}
document.addEventListener("click",e=>{
  const b=e.target.closest("button[data-vi]"); if(!b||!rep) return;
  const i=+b.dataset.vi;
  if(b.dataset.k==="X") rep.viol.splice(i,1); else rep.viol[i].kind=b.dataset.k;
  renderViol(); save();
});
function renderOv(q){
  q=(q||"").trim().toLowerCase();
  const picked=new Set(rep?rep.viol.map(v=>v.text):[]);
  let html="", any=false;
  VIOLATION_STEPS.forEach(sec=>{
    const items=sec.items.filter(v=>!q||v.toLowerCase().includes(q));
    if(!items.length) return;
    any=true;
    html += `<div class="stephead"><b>${esc(sec.step)}</b> — ${esc(sec.head)}</div>`;
    html += items.map(v=>{
      const p=splitV(v);
      return `<button class="vpick ${picked.has(v)?"picked":""}" data-add="${esc(v)}">${esc(p.t)}<code>${esc(p.c)}</code></button>`;
    }).join("");
  });
  $("ovList").innerHTML = any ? html : `<p class="empty">Nothing matches.</p>`;
}
$("openOv").onclick=()=>{ if(!cur) return openStage("stAcct");
  $("ovSearch").value=""; renderOv(""); $("ov").classList.add("on"); };
$("ovClose").onclick=()=>$("ov").classList.remove("on");
$("ovSearch").addEventListener("input",e=>renderOv(e.target.value));
$("ovList").addEventListener("click",e=>{
  const b=e.target.closest("[data-add]"); if(!b||!rep) return;
  const t=b.dataset.add, at=rep.viol.findIndex(v=>v.text===t);
  if(at>=0) rep.viol.splice(at,1); else rep.viol.push({text:t,kind:"V"});
  renderOv($("ovSearch").value); renderViol(); save();
});

function renderAll(){
  if(!cur) return;
  ["dateInsp","certExp","test1","test5","notes"].forEach(k=>$(k).value=rep[k]||"");
  renderGroups(); renderCarry(); renderViol(); applyMode();
}
["dateInsp","certExp","test1","test5","notes"].forEach(k=>
  $(k).addEventListener("input",()=>{ if(rep){ rep[k]=$(k).value; save(); } }));

/* ---------- save / save & lock ---------- */
$("btnSave").onclick=()=>{ if(!cur) return openStage("stAcct"); save(); };
$("btnLock").onclick=()=>{
  if(!cur) return openStage("stAcct");
  if(!confirm(`Save ${cur.loc} (Oklahoma #${cur.okla}) and lock the app?`)) return;
  save();
  lockApp();
};

/* ---------- connection ---------- */
window.addEventListener("online",()=>{ setStatus("Back on wifi. Checking your other device...","wait"); syncBoot(); });
window.addEventListener("offline",()=>setStatus("No connection. Keep working, it is held on this phone.","wait"));

/* ---------- boot ---------- */
function boot(){
  ACCOUNTS = buildAccounts();
  applyMode();
  if(!ELEVATORS.length){ showImport(); return; }
  $("importCard").classList.add("hide");
  $("shell").querySelector(".selector").classList.remove("hide");
  renderAccts("");
  setStatus(`${ELEVATORS.length} elevators across ${ACCOUNTS.length} accounts. Pick an account to start.`,"ok");
}
function showImport(){
  $("importCard").classList.remove("hide");
  $("shell").querySelector(".selector").classList.add("hide");
  setStatus("No elevator list on this device yet. Load the spreadsheet to start.","wait");
}
$("importFile").addEventListener("change", e=>{
  const f = e.target.files && e.target.files[0]; if(!f) return;
  const fr = new FileReader();
  fr.onload = ()=>{
    const list = parseCsv(String(fr.result));
    if(!list.length){ $("importMsg").textContent = "No elevator rows found in that file. It should be the spreadsheet saved as CSV."; return; }
    setRoster(list);
    $("importMsg").textContent = "";
    boot();
  };
  fr.onerror = ()=>{ $("importMsg").textContent = "That file could not be read. Try saving it as CSV again."; };
  fr.readAsText(f);
});
